import "dotenv/config"
import utils from "./utils"

const start = async () => {
    let pixivKey = process.env.PIXIV_REFRESH_TOKEN!
    let processFolder = process.env.PROCESS_FOLDER!
    let saucenaoKey = process.env.SAUCENAO_KEY!
}

start()